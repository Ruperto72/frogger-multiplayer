# Språkval (sv/en) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Tvåspråkigt UI (svenska/engelska) med växlare på startskärmen; standard via `navigator.language`, valet sparas i localStorage.

**Architecture:** Egen minimal strängtabell i ny modul `frontend/js/i18n.js` (`t(key, vars?)`, `setLang`, `applyStatic`, `detectLang`). Statiska HTML-texter via `data-i18n`-attribut; dynamiska texter byter till `t()`-anrop och följer med automatiskt eftersom de renderas om varje frame. Serverns standardnamn blir språkneutralt engelska. Spec: `docs/superpowers/specs/2026-07-05-sprakval-design.md`.

**Tech Stack:** Vanilla JS ES6-moduler (ingen byggprocess), Node.js `node --test` för DOM-fria delar.

## Global Constraints

- Inga nya npm-beroenden. Ingen byggprocess.
- Testkommando: `cd backend && node --test test/*.test.js` (glob-form).
- `frontend/js/sim.js`, `backend/gameloop.js`, `backend/collision.js` får INTE ändras.
- `i18n.js` MÅSTE gå att importera i Node utan DOM (typeof-vakter för `localStorage`/`navigator`/`document`) — testas via dynamic import från backend-sviten.
- Språkkoder: exakt `'sv'` och `'en'`. localStorage-nyckel: `'lang'`. Detektion: `navigator.language`-prefix `sv` → svenska, annat → engelska; sparat giltigt val vinner.
- Servern skickar språkneutrala felkoder (`unknown_code` m.fl.) — protokollet ändras INTE.
- Kommentarer bara där något är icke-uppenbart. Ändra inte mer än uppgiften kräver.
- Varje commit direkt på `master`, avslutas med `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`. Push sker först i Task 5.
- `CLAUDE.md` är gitignorerad i detta repo — uppdateras BARA på disk, ALDRIG `git add -f`. `.superpowers/` committas aldrig.

---

### Task 1: `frontend/js/i18n.js` + tester

Hela strängtabellen och den DOM-fria kärnan. Alla nycklar för senare tasks
definieras här — sv- och en-ordböckerna måste ha identisk nyckeluppsättning
(paritetstestet låser detta).

**Files:**
- Create: `frontend/js/i18n.js`
- Test: `backend/test/i18n.test.js`

**Interfaces:**
- Produces: `t(key, vars?) → string` (interpolerar `{x}` från vars; fallback en→sv→nyckeln), `getLang() → 'sv'|'en'`, `setLang(l)` (validerar, sparar i localStorage, kör applyStatic), `applyStatic()` (uppdaterar `[data-i18n]`/`[data-i18n-placeholder]`/`[data-i18n-aria]` + `<html lang>`), `detectLang(saved, navLang)` (ren), `LANGS` (exporteras för paritetstestet). Används av Task 2–4.

- [ ] **Step 1: Skriv failande tester**

Skapa `backend/test/i18n.test.js`:

```js
const { test } = require('node:test');
const assert = require('node:assert/strict');

async function makeI18n() {
  // Cache-bust så varje test får färskt modul-state (lang är modulglobal)
  return import(`../../frontend/js/i18n.js?${Math.random()}`);
}

test('detectLang: sparat giltigt val vinner över webbläsarspråk', async () => {
  const { detectLang } = await makeI18n();
  assert.equal(detectLang('sv', 'en-US'), 'sv');
  assert.equal(detectLang('en', 'sv-SE'), 'en');
});

test('detectLang: sv-prefix ger svenska, allt annat engelska', async () => {
  const { detectLang } = await makeI18n();
  assert.equal(detectLang(null, 'sv-SE'), 'sv');
  assert.equal(detectLang(null, 'sv'), 'sv');
  assert.equal(detectLang(null, 'en-GB'), 'en');
  assert.equal(detectLang(null, 'de-DE'), 'en');
  assert.equal(detectLang(null, undefined), 'en');
  assert.equal(detectLang('xx', 'de'), 'en'); // ogiltigt sparat val ignoreras
});

test('t: slår upp i aktivt språk och interpolerar variabler', async () => {
  const { t, setLang } = await makeI18n();
  setLang('sv');
  assert.equal(t('start.players', { n: 8 }), '8 spelare');
  assert.equal(t('t.joined', { count: 3, size: 8 }), '3 av 8 anslutna');
  setLang('en');
  assert.equal(t('start.players', { n: 8 }), '8 players');
  assert.equal(t('t.joined', { count: 3, size: 8 }), '3 of 8 joined');
});

test('t: okänd nyckel returnerar nyckeln själv', async () => {
  const { t } = await makeI18n();
  assert.equal(t('finns.inte'), 'finns.inte');
});

test('setLang: ogiltigt språk ignoreras', async () => {
  const { setLang, getLang } = await makeI18n();
  setLang('en');
  setLang('xx');
  assert.equal(getLang(), 'en');
});

test('paritet: sv och en har exakt samma nycklar', async () => {
  const { LANGS } = await makeI18n();
  assert.deepEqual(Object.keys(LANGS.sv).sort(), Object.keys(LANGS.en).sort());
});
```

- [ ] **Step 2: Kör testet och verifiera att det failar**

Kör: `cd backend && node --test test/i18n.test.js`
Förväntat: FAIL med `Cannot find module`/`ERR_MODULE_NOT_FOUND`.

- [ ] **Step 3: Implementera `frontend/js/i18n.js`**

```js
// Strängtabell + språkval. Kärnan (t, detectLang) är DOM-fri och testas från
// backend-sviten — därav typeof-vakterna: modulen måste gå att importera i Node.

export const LANGS = {
  sv: {
    'start.namePlaceholder': 'Ditt namn',
    'start.quick': 'Snabbmatch',
    'start.create': 'Skapa turnering',
    'start.join': 'Gå med',
    'start.codePlaceholder': 'KOD',
    'start.players': '{n} spelare',
    'start.bestof1': 'Bäst av 1',
    'start.bestof3': 'Bäst av 3',
    'start.bestof5': 'Bäst av 5',
    'skin.green': 'Grön',
    'skin.yellow': 'Gul',
    'skin.blue': 'Blå',
    'error.unknown_code': 'Ingen turnering med den koden.',
    'error.tournament_full': 'Turneringen är full.',
    'error.name_taken': 'Namnet är upptaget — välj ett annat.',
    'error.already_started': 'Turneringen har redan startat.',
    'error.tournament_cancelled': 'Värden lämnade — turneringen avbröts.',
    'error.connection_lost': 'Anslutningen bröts. Välj läge för att börja om.',
    'lobby.waiting': 'Väntar på motspelare…',
    'lobby.waitReady': 'Väntar på att motståndaren blir redo…',
    'lobby.found': 'Motspelare hittad — gör dig redo!',
    'lobby.ready': 'redo ✓',
    'lobby.notReady': 'inte redo',
    'lobby.readyBtn': 'Redo',
    't.title': 'Turnering',
    't.codeLabel': 'Kod:',
    't.copy': 'Kopiera länk',
    't.start': 'Lotta & starta',
    't.host': ' (värd)',
    't.joined': '{count} av {size} anslutna',
    't.champion': '🏆 {name} vann turneringen! Ladda om sidan för att spela igen.',
    't.yourMatch': 'Din match står på tur — gör dig redo!',
    't.waitOpponent': 'Väntar på motståndaren…',
    't.spectator': 'Du är åskådare i nästa match.',
    't.bye': '(frilott)',
    't.walkover': 'walkover',
    'game.you': 'DU',
    'game.youShort': 'Du',
    'game.oppShort': 'Motst',
    'game.getReady': 'Gör dig redo!',
    'game.round': 'Runda',
    'game.goals': 'Mål',
    'game.match': 'Match',
    'game.spectatorLabel': 'Åskådare',
    'game.wonRoundYou': 'Du vann rundan! 🐸',
    'game.wonRoundOther': '{name} vann rundan',
    'game.nextRound': 'Nästa runda startar…',
    'game.wonMatchYou': 'Du vann matchen! 🏆',
    'game.wonMatchOther': '{name} vann matchen',
    'game.result': 'Resultat: {score}',
    'game.opponentLeft': 'Motspelaren kopplade från',
    'game.reload': 'Ladda om sidan för ny match',
    'game.opponentFallback': 'Motspelaren',
    'touch.left': 'Vänster',
    'touch.right': 'Höger',
    'touch.up': 'Fram',
    'touch.down': 'Bak'
  },
  en: {
    'start.namePlaceholder': 'Your name',
    'start.quick': 'Quick match',
    'start.create': 'Create tournament',
    'start.join': 'Join',
    'start.codePlaceholder': 'CODE',
    'start.players': '{n} players',
    'start.bestof1': 'Best of 1',
    'start.bestof3': 'Best of 3',
    'start.bestof5': 'Best of 5',
    'skin.green': 'Green',
    'skin.yellow': 'Yellow',
    'skin.blue': 'Blue',
    'error.unknown_code': 'No tournament with that code.',
    'error.tournament_full': 'The tournament is full.',
    'error.name_taken': 'That name is taken — pick another.',
    'error.already_started': 'The tournament has already started.',
    'error.tournament_cancelled': 'The host left — the tournament was cancelled.',
    'error.connection_lost': 'Connection lost. Pick a mode to start over.',
    'lobby.waiting': 'Waiting for an opponent…',
    'lobby.waitReady': 'Waiting for your opponent to ready up…',
    'lobby.found': 'Opponent found — get ready!',
    'lobby.ready': 'ready ✓',
    'lobby.notReady': 'not ready',
    'lobby.readyBtn': 'Ready',
    't.title': 'Tournament',
    't.codeLabel': 'Code:',
    't.copy': 'Copy link',
    't.start': 'Draw & start',
    't.host': ' (host)',
    't.joined': '{count} of {size} joined',
    't.champion': '🏆 {name} won the tournament! Reload the page to play again.',
    't.yourMatch': 'Your match is up — get ready!',
    't.waitOpponent': 'Waiting for your opponent…',
    't.spectator': 'You are spectating the next match.',
    't.bye': '(bye)',
    't.walkover': 'walkover',
    'game.you': 'YOU',
    'game.youShort': 'You',
    'game.oppShort': 'Opp',
    'game.getReady': 'Get ready!',
    'game.round': 'Round',
    'game.goals': 'Goals',
    'game.match': 'Match',
    'game.spectatorLabel': 'Spectator',
    'game.wonRoundYou': 'You won the round! 🐸',
    'game.wonRoundOther': '{name} won the round',
    'game.nextRound': 'Next round starting…',
    'game.wonMatchYou': 'You won the match! 🏆',
    'game.wonMatchOther': '{name} won the match',
    'game.result': 'Result: {score}',
    'game.opponentLeft': 'Your opponent disconnected',
    'game.reload': 'Reload the page for a new match',
    'game.opponentFallback': 'Your opponent',
    'touch.left': 'Left',
    'touch.right': 'Right',
    'touch.up': 'Up',
    'touch.down': 'Down'
  }
};

export function detectLang(saved, navLang) {
  if (saved === 'sv' || saved === 'en') return saved;
  return String(navLang ?? '').toLowerCase().startsWith('sv') ? 'sv' : 'en';
}

let lang = detectLang(
  typeof localStorage !== 'undefined' ? localStorage.getItem('lang') : null,
  typeof navigator !== 'undefined' ? navigator.language : ''
);

export function getLang() {
  return lang;
}

export function t(key, vars) {
  let s = LANGS[lang][key] ?? LANGS.sv[key] ?? key;
  if (vars) {
    for (const [k, v] of Object.entries(vars)) s = s.replaceAll(`{${k}}`, v);
  }
  return s;
}

export function setLang(l) {
  if (l !== 'sv' && l !== 'en') return;
  lang = l;
  if (typeof localStorage !== 'undefined') localStorage.setItem('lang', l);
  applyStatic();
}

export function applyStatic() {
  if (typeof document === 'undefined') return;
  document.documentElement.lang = lang;
  for (const el of document.querySelectorAll('[data-i18n]')) {
    el.textContent = t(el.dataset.i18n);
  }
  for (const el of document.querySelectorAll('[data-i18n-placeholder]')) {
    el.placeholder = t(el.dataset.i18nPlaceholder);
  }
  for (const el of document.querySelectorAll('[data-i18n-aria]')) {
    el.setAttribute('aria-label', t(el.dataset.i18nAria));
  }
}
```

- [ ] **Step 4: Kör testet och verifiera att det passerar**

Kör: `cd backend && node --test test/i18n.test.js`
Förväntat: PASS, 6 tester.

- [ ] **Step 5: Kör hela testsviten**

Kör: `cd backend && node --test test/*.test.js`
Förväntat: PASS, inga regressioner.

- [ ] **Step 6: Commit**

```bash
git add frontend/js/i18n.js backend/test/i18n.test.js
git commit -m "feat: i18n-modul med sv/en-strängtabell och språkdetektion

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 2: Startskärm — data-i18n, växlare och dynamiska alternativ

**Files:**
- Modify: `frontend/index.html`
- Modify: `frontend/style.css`
- Modify: `frontend/js/start-ui.js`
- Modify: `frontend/js/main.js`

**Interfaces:**
- Consumes: `t`, `setLang`, `getLang`, `applyStatic` från `./i18n.js` (Task 1).

- [ ] **Step 1: Uppdatera `frontend/index.html`**

Lägg till växlaren först i `#start`-panelen (direkt efter `<div id="start" class="lobby">`):

```html
    <div class="lang-switch">
      <button id="lang-sv">SV</button>
      <button id="lang-en">EN</button>
    </div>
```

Sätt sedan data-i18n-attribut på befintliga element (texterna behålls som
svensk fallback tills `applyStatic()` körs vid boot):

- `#start-name`: lägg till `data-i18n-placeholder="start.namePlaceholder"`
- Alla sex skin-knappar (i `#start-skins` och `#lobby-skins`): `data-i18n-aria="skin.green"` / `"skin.yellow"` / `"skin.blue"` på respektive knapp
- `#start-quick`: `data-i18n="start.quick"`
- Bestof-alternativen: `<option value="1" data-i18n="start.bestof1">Bäst av 1</option>` osv för 3 och 5 (behåll `selected` på 3)
- `#start-create`: `data-i18n="start.create"`
- `#start-code`: `data-i18n-placeholder="start.codePlaceholder"`
- `#start-join`: `data-i18n="start.join"`
- `#lobby-status`: `data-i18n="lobby.waiting"` (uppdateras ändå dynamiskt av lobby-ui, men attributet ger rätt text före första update)
- `#lobby-name`: `data-i18n-placeholder="start.namePlaceholder"`
- `#lobby-ready`: `data-i18n="lobby.readyBtn"`
- `#tournament h1`: `data-i18n="t.title"`
- I `.code-row`: wrappa ledtexten i en span — ersätt `Kod: <strong id="t-code">` med `<span data-i18n="t.codeLabel">Kod:</span> <strong id="t-code">` (data-i18n får INTE sitta på `<p>` — det skulle radera strong/knapp-barnen)
- `#t-copy`: `data-i18n="t.copy"`
- `#t-start`: `data-i18n="t.start"`
- `#t-ready`: `data-i18n="lobby.readyBtn"`
- Touchknapparna: `data-i18n-aria="touch.left"` / `"touch.right"` / `"touch.up"` / `"touch.down"`

`lang="sv"` på `<html>` lämnas som fallback — `applyStatic()` sätter rätt värde vid boot.

- [ ] **Step 2: Lägg till växlar-CSS sist i `frontend/style.css`**

```css
.lang-switch {
  position: absolute;
  top: 1rem;
  right: 1rem;
  display: flex;
  gap: 0.25rem;
}

.lang-switch button {
  font: inherit;
  padding: 0.25rem 0.6rem;
  background: #222;
  color: #888;
  border: 1px solid #555;
  border-radius: 4px;
  cursor: pointer;
}

.lang-switch button.active {
  color: #fff;
  border-color: #fff;
}
```

- [ ] **Step 3: Uppdatera `frontend/js/start-ui.js`**

Överst i filen: ersätt hela `ERROR_TEXTS`-konstanten (rad 1–8) med:

```js
import { t, setLang, getLang } from './i18n.js';
```

I konstruktorn: ersätt options-loopen (rad 24–30, `for (let n = 2; ...)`) med:

```js
    this._fillSizeOptions();
```

Lägg till växlar-wiring sist i konstruktorn (efter join-lyssnaren):

```js
    this._langBtns = {
      sv: document.getElementById('lang-sv'),
      en: document.getElementById('lang-en')
    };
    for (const [l, btn] of Object.entries(this._langBtns)) {
      btn.addEventListener('click', () => {
        setLang(l);
        this._onLangChange();
      });
    }
    this._onLangChange();
```

Nya metoder (före `_saveProfile`):

```js
  _onLangChange() {
    for (const [l, btn] of Object.entries(this._langBtns)) {
      btn.classList.toggle('active', l === getLang());
    }
    this._fillSizeOptions();
  }

  _fillSizeOptions() {
    const current = this._size.value || '8';
    this._size.replaceChildren();
    for (let n = 2; n <= 16; n++) {
      const opt = document.createElement('option');
      opt.value = n;
      opt.textContent = t('start.players', { n });
      if (String(n) === current) opt.selected = true;
      this._size.appendChild(opt);
    }
  }
```

I `update()`: ersätt error-raden `const text = ERROR_TEXTS[this._state.lastError] ?? '';` med:

```js
    const text = this._state.lastError ? t(`error.${this._state.lastError}`) : '';
```

(Obs: `t()` returnerar nyckeln själv för okända koder — acceptabelt för
oförutsedda serverfel.)

- [ ] **Step 4: Koppla in `applyStatic` i `frontend/js/main.js`**

Lägg till import:

```js
import { applyStatic } from './i18n.js';
```

Direkt före `function loop() {`:

```js
applyStatic();
```

- [ ] **Step 5: Verifiera**

Kör: `node --check frontend/js/start-ui.js && node --check frontend/js/main.js && node --check frontend/js/i18n.js`
Förväntat: inga fel.
Kör: `cd backend && node --test test/*.test.js` — förväntat PASS.
Manuell webbläsarkontroll (om möjligt, annars deferred): startskärmen visar EN-texter i icke-svensk webbläsare; SV/EN-knapparna växlar alla statiska texter + "N spelare"-alternativen; valet överlever omladdning.

- [ ] **Step 6: Commit**

```bash
git add frontend/index.html frontend/style.css frontend/js/start-ui.js frontend/js/main.js
git commit -m "feat: språkväxlare och data-i18n på startskärm/paneler

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 3: Lobbypanel och turneringspanel via `t()`

**Files:**
- Modify: `frontend/js/lobby-ui.js`
- Modify: `frontend/js/tournament-ui.js`

**Interfaces:**
- Consumes: `t`, `getLang` från `./i18n.js` (Task 1).

- [ ] **Step 1: Uppdatera `frontend/js/lobby-ui.js`**

Lägg till import överst:

```js
import { t } from './i18n.js';
```

I `update()`, byt de fyra svenska strängarna:

```js
      setText(this._status, t('lobby.waiting'));
```

```js
      setText(this._status, this._sent ? t('lobby.waitReady') : t('lobby.found'));
      setText(this._opponent, op ? `${op.name}: ${op.ready ? t('lobby.ready') : t('lobby.notReady')}` : '');
```

- [ ] **Step 2: Uppdatera `frontend/js/tournament-ui.js`**

Lägg till import överst (alias — lokala variabeln `t` är turneringsstate):

```js
import { t as tr, getLang } from './i18n.js';
```

I `update()`: språket in i render-cachenyckeln (annars ritas panelen inte om
vid växling):

```js
    const json = JSON.stringify([t, s.phase, this._sentReadyFor, getLang()]);
```

Byt strängarna:

```js
        el('p', null, `${p.name}${p.isHost ? tr('t.host') : ''}`)));
      this._status.textContent = tr('t.joined', { count: t.participants.length, size: t.size });
```

```js
      this._status.textContent = tr('t.champion', { name: champ?.name ?? '?' });
```

```js
    this._status.textContent = inMatch
      ? (showReady ? tr('t.yourMatch') : tr('t.waitOpponent'))
      : tr('t.spectator');
```

I `_renderBracket`:

```js
    const nameOf = (id) => id == null
      ? tr('t.bye')
      : (t.participants.find(p => p.id === id)?.name ?? '?');
```

```js
        if (m.walkover && m.p2 != null) box.appendChild(el('div', 'b-note', tr('t.walkover')));
```

- [ ] **Step 3: Verifiera**

Kör: `node --check frontend/js/lobby-ui.js && node --check frontend/js/tournament-ui.js`
Förväntat: inga fel.
Kör: `cd backend && node --test test/*.test.js` — förväntat PASS.

- [ ] **Step 4: Commit**

```bash
git add frontend/js/lobby-ui.js frontend/js/tournament-ui.js
git commit -m "feat: lobby- och turneringspanel via i18n

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 4: Canvas-texter via `t()`

**Files:**
- Modify: `frontend/js/renderer.js`

**Interfaces:**
- Consumes: `t` från `./i18n.js` (Task 1).

- [ ] **Step 1: Uppdatera `frontend/js/renderer.js`**

Lägg till import överst (efter befintliga konstanter går bra, före klassen):

```js
import { t } from './i18n.js';
```

I `draw()`, byt overlay-strängarna:

```js
    if (state.phase === 'countdown') {
      const n = Math.max(1, Math.ceil(state.countdownRemaining() / 1000));
      this._drawOverlay(String(n), t('game.getReady'));
    }
    if (state.phase === 'round_over') {
      const w = state.lastEvent?.winner;
      this._drawOverlay(
        w === state.you ? t('game.wonRoundYou') : t('game.wonRoundOther', { name: this._name(state, w) }),
        t('game.nextRound')
      );
    }
    if (state.phase === 'match_over') {
      const w = state.lastEvent?.winner;
      this._drawOverlay(
        w === state.you ? t('game.wonMatchYou') : t('game.wonMatchOther', { name: this._name(state, w) }),
        t('game.result', { score: state.lastEvent?.score?.join(' – ') ?? '' })
      );
    }
    if (state.phase === 'disconnected') {
      this._drawOverlay(t('game.opponentLeft'), t('game.reload'));
    }
```

I `_name()`:

```js
  _name(state, pid) {
    return state.players[pid]?.name ?? t('game.opponentFallback');
  }
```

I `_drawPlayers()`, etikettraden:

```js
      ctx.fillText(pid === state.you ? t('game.you') : pid.toUpperCase(),
        p.x * cell + cell / 2, p.y * cell + cell / 2);
```

I `_drawHUD()`, åskådargrenen:

```js
        ctx.fillText(
          `${t('game.spectatorLabel')}  |  ${t('game.round')} ${state.round}  |  ${a.name}: ♥${a.lives} ${t('game.goals')}:${a.score}  |  ${b.name}: ♥${b.lives} ${t('game.goals')}:${b.score}  |  ${t('game.match')}: ${state.roundScores.p1}–${state.roundScores.p2}`,
          8, 16
        );
```

och spelargrenen:

```js
      ctx.fillText(
        `${t('game.round')} ${state.round}  |  ${pYou.name ?? t('game.youShort')}: ♥${pYou.lives} ${t('game.goals')}:${pYou.score}  |  ${pOther.name ?? t('game.oppShort')}: ♥${pOther.lives} ${t('game.goals')}:${pOther.score}  |  ${t('game.match')}: ${state.roundScores[you]}–${state.roundScores[other]}`,
        8, 16
      );
```

- [ ] **Step 2: Verifiera**

Kör: `node --check frontend/js/renderer.js`
Förväntat: inga fel.
Kör: `cd backend && node --test test/*.test.js` — förväntat PASS.

- [ ] **Step 3: Commit**

```bash
git add frontend/js/renderer.js
git commit -m "feat: canvas-texter via i18n

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 5: Språkneutrala servernamn, dokumentation och push

**Files:**
- Modify: `backend/constants.js:15`
- Modify: `backend/tournament.js:30`
- Modify: `backend/test/room.test.js:138-139,168,202`
- Modify: `CLAUDE.md` (ENDAST på disk — filen är gitignorerad, ALDRIG `git add -f`)

**Interfaces:**
- Consumes: inga. Namnen är delad data som visas för alla klienter oavsett språk.

- [ ] **Step 1: Uppdatera standardnamnen**

`backend/constants.js` rad 15:

```js
const DEFAULT_NAMES = { p1: 'Player 1', p2: 'Player 2' };
```

`backend/tournament.js` rad 30:

```js
    name = String(name ?? '').trim().slice(0, NAME_MAX_LEN) || `Player ${this._nextId}`;
```

- [ ] **Step 2: Uppdatera testerna som asserterar standardnamn**

I `backend/test/room.test.js`: byt `'Spelare 1'` → `'Player 1'` och
`'Spelare 2'` → `'Player 2'` på raderna 138, 139, 168 och 202 (fyra
assert-rader; övriga `Spelare`-förekomster i testfilerna är användarinmatade
namn och ska INTE ändras).

- [ ] **Step 3: Kör hela testsviten**

Kör: `cd backend && node --test test/*.test.js`
Förväntat: PASS, alla tester.

- [ ] **Step 4: Uppdatera `CLAUDE.md` på disk (INTE commit)**

I frontend-fillistan, lägg till efter raden om `sim.js`:

```
i18n.js        Sv/en-strängtabell: t(key, vars), setLang/getLang, applyStatic (data-i18n-attribut), detectLang
```

Lägg till sist i frontend-avsnittet:

```
**Språk:** UI:t är tvåspråkigt (sv/en) via `i18n.js`. Standard från `navigator.language`
(sv → svenska, annars engelska), aktivt val i `localStorage('lang')`. Statiska texter:
`data-i18n`/`data-i18n-placeholder`/`data-i18n-aria`-attribut + `applyStatic()`. Dynamiska
texter: `t()`-anrop i rAF-renderade updates. Nya UI-strängar läggs i BÅDA ordböckerna i
`i18n.js` (paritetstest låser nyckeluppsättningen). Serverns standardnamn är engelska
(`Player 1/2`, `Player N`) — delad data översätts inte per klient.
```

- [ ] **Step 5: Commit och push**

```bash
git add backend/constants.js backend/tournament.js backend/test/room.test.js
git commit -m "feat: språkneutrala standardnamn (Player 1/2)

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
git push
```

Verifiera att `git status --short` INTE visar CLAUDE.md som staged (den ska
förbli ospårad). Push till master triggar GitHub Pages- och Render-deploy;
ingen service worker — ingen cache-bump behövs.
